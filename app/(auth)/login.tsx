import LoginScreen from "@/screens/Login";
import { SafeAreaView } from "react-native";

export default function Login() {
  return (
    <SafeAreaView className="bg-tertiary-400/60">
      <LoginScreen />
    </SafeAreaView>
  );
}
